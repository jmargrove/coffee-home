import React, { FunctionComponent } from "react"
import { Image } from "react-native"
const source = require("./console-right-arrow.png")

export const ConsoleRightArrow: FunctionComponent = () => {
  return <Image source={source} />
}
